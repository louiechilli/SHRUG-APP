# Nginx for serving Laravel
FROM nginx:alpine

# Copy nginx configuration
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf

# Copy public files (will be mounted in docker-compose)
WORKDIR /var/www/html

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]


